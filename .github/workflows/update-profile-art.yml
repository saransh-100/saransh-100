name: update-profile-art

on:
  schedule:
    - cron: "20 18 * * *" # daily, ~04:20 AEST
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r scripts/requirements.txt

      - name: Refresh contribution heatmap
        run: |
          python scripts/fetch_contributions.py
          python scripts/render_heatmap_svg.py

      # uptime + stats change daily
      - name: Refresh neofetch card
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python scripts/make_neofetch_svg.py

      # the portrait and project cards only change with their sources — regenerate on manual runs
      - name: Regenerate ASCII portrait and project cards
        if: github.event_name == 'workflow_dispatch'
        run: |
          python scripts/make_ascii_svg.py
          python scripts/make_project_cards.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data assets
          git diff --cached --quiet || (git commit -m "chore: refresh profile art" && git push)
